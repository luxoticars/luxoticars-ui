declare module "virtual:astro-story/*" {
  /** Scoped CSS Astro compiled for the component. */
  export const css: string;
  /** Variant name to pre-rendered HTML. */
  export const variants: Record<string, string>;
  export const variantNames: string[];
}
